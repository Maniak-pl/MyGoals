<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Match">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="Wrap">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="Empty">
        <item name="android:layout_width">@dimen/empty</item>
        <item name="android:layout_height">@dimen/empty</item>
    </style>

    <style name="MatchWrap" parent="Match">
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="WrapMatch" parent="Wrap">
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="GuidelineVertical" parent="MatchWrap">
        <item name="android:orientation">vertical</item>
    </style>

    <style name="GuidelineHorizontal" parent="WrapMatch">
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="GoalProgressBar" parent="@style/Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/goal_progressbar_red</item>
        <item name="android:minWidth">200dp</item>
    </style>

    <style name="ColorSpinner">
        <item name="android:dropDownSelector">@android:color/transparent</item>
        <item name="android:spinnerMode">dropdown</item>
    </style>
</resources>
